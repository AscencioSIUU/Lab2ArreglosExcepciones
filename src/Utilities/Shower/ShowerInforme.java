package Utilities.Shower;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;

import Utilities.Seachers.EventosSearcher;

public class ShowerInforme {
    /*
     * public void ShowInformation(EventosSearcher Eventos) throws
     * IOException {
     * String file = "src/DB/Eventos.csv";
     * String delimiter = ",";
     * String line = "";
     * 
     * try ( BufferedReader csvReader = new BufferedReader(new FileReader(file))){
     * line = csvReader.readLine();
     * String[] headers = line.split(delimiter);
     * System.out.printf("%-20s %-20s %-20s %-20s %-20s %-20s%n", headers[0],
     * headers[1], headers[2], headers[3], headers[4], headers[5]);
     * 
     * while ((line = csvReader.readLine()) != null) {
     * String[] data = line.split(delimiter);
     * System.out.printf("%-20s %-20s %-20s %-20s %-20s %-20s%n", headers[0],
     * headers[1], headers[2], headers[3], headers[4], headers[5]);
     * }
     * } catch (IOException e) {
     * e.printStackTrace();
     * }
     * }
     */

}
